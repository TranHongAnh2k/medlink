import styled from "styled-components";

export const Container = styled.div`
    background-color: var(--bs-cyan);
    opacity: .9;
    min-height: 100vh;
    align-items: center;
    margin: 0 auto;
   
`

